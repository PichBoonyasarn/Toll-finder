services:
  - type: web
    name: toll-finder
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GOOGLE_MAPS_KEY
        sync: false
      - key: COMPANY_ADDRESS
        sync: false
